void main() {
  int no_classes_held = 16;
  int no_classes_attened = 10;

  double std_percentage_of_attandence = (10 / 16) * 100;

  if (std_percentage_of_attandence >= 75) {
    print("std allow to sit in Exam :");
  } else {
    print("std not allow to sit in Exam");
  }
}
